<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Link Wrapper"
        android:theme="@style/Theme.LinkWrapper"
        android:usesCleartextTraffic="false">

        <!-- Domovská obrazovka appky (spouští se z launcheru) -->
        <activity
            android:name=".HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Obrazovka, která zobrazí odkaz ve WebView -->
        <activity
            android:name=".WebViewActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <!--
                  Zatím appka přijímá jakoukoliv http/https adresu, aby se
                  objevila v systémovém dialogu "Otevřít pomocí".
                  Až budete znát konkrétní firemní doménu, přidejte sem
                  <data android:scheme="https" android:host="vase-domena.cz" />
                  a appka pak nabídne otevření jen pro tuhle doménu.
                -->
            </intent-filter>
        </activity>

        <!-- Historie otevřených odkazů -->
        <activity
            android:name=".HistoryActivity"
            android:exported="false" />

    </application>
</manifest>
